class Address {
  int id;
  final String addressTitle;
  final String country;
  final String province;
  final String zip;
  final String detail;

  Address(this.id, this.addressTitle, this.country, this.province, this.zip,
      this.detail);
}
